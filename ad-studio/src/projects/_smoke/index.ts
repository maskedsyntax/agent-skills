import {Ad} from './Ad';
import type {Project} from '../types';

export const project: Project = {
  id: 'smoke-test',
  component: Ad,
  durationInFrames: 90,
  fps: 30,
  width: 1080,
  height: 1920,
};
